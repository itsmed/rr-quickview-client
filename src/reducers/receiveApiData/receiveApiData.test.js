import {
  RECEIVE_USERS,
  RECEIVE_EMPLOYEES,
  RECEIVE_TRANSACTIONS,
  RECEIVE_FILTERED_RESULTS,
} from '../../actions/types';

import { receiveApiData } from './receiveApiData';

test('Something', () => {
  expect(4 * 4).toBeGreaterThan(0)
})